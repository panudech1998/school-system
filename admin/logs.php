<?php

declare(strict_types=1);require_once __DIR__.'/../includes/layout.php';require_admin();$audits=db()->query('SELECT a.*,u.name user_name FROM audit_logs a LEFT JOIN users u ON u.id=a.user_id ORDER BY a.id DESC LIMIT 200')->fetchAll();$searches=db()->query('SELECT l.*,e.title event_title FROM face_search_logs l JOIN events e ON e.id=l.event_id ORDER BY l.id DESC LIMIT 100')->fetchAll();page_header('ประวัติ',true);require __DIR__.'/_nav.php';?><h1>ประวัติระบบ</h1><h2>การจัดการหลังบ้าน</h2><div class="table-wrap"><table><thead><tr><th>เวลา</th><th>ผู้ใช้</th><th>รายการ</th><th>รายละเอียด</th></tr></thead><tbody><?php foreach($audits as $row):?><tr><td><?=e($row['created_at'])?></td><td><?=e($row['user_name']??'-')?></td><td><?=e($row['action_name'])?></td><td><?=e($row['detail_text'])?></td></tr><?php endforeach?></tbody></table></div><h2>การค้นหาใบหน้า</h2><div class="table-wrap"><table><thead><tr><th>เวลา</th><th>กิจกรรม</th><th>ผลลัพธ์</th><th>เกณฑ์</th></tr></thead><tbody><?php foreach($searches as $row):?><tr><td><?=e($row['created_at'])?></td><td><?=e($row['event_title'])?></td><td><?=(int)$row['result_count']?></td><td><?=e($row['threshold'])?></td></tr><?php endforeach?></tbody></table></div><?php page_footer();?>
